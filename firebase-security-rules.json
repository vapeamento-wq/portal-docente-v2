{
    "rules": {
        "docentes": {
            "$docenteId": {
                ".read": true,
                ".write": "auth !== null && auth.provider !== 'anonymous'"
            }
        },
        "logs": {
            ".read": "auth !== null && auth.provider !== 'anonymous'",
            ".write": "auth !== null"
        },
        "analytics": {
            ".read": "auth !== null && auth.provider !== 'anonymous'",
            ".write": "auth !== null"
        },
        "config": {
            ".read": true,
            ".write": "auth !== null && auth.provider !== 'anonymous'"
        }
    }
}