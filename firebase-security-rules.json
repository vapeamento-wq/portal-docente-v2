{
    "rules": {
        "docentes": {
            ".read": true,
            ".write": "auth !== null && auth.provider !== 'anonymous'"
        },
        "logs": {
            ".read": "auth !== null && auth.provider !== 'anonymous'",
            ".write": "auth !== null"
        },
        "analytics": {
            ".read": true,
            ".write": "auth !== null"
        },
        "config": {
            ".read": true,
            ".write": "auth !== null && auth.provider !== 'anonymous'"
        },
        "estudiantes": {
            ".read": "auth !== null && auth.provider !== 'anonymous'",
            ".write": "auth !== null && auth.provider !== 'anonymous'"
        }
    }
}